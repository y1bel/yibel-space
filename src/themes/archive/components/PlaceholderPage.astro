---
import type { PageData } from "@core/theme";

interface Props {
  data: PageData;
}

const { data } = Astro.props;
const Body = data.kind === "post" ? data.post.body : undefined;
---

<main>
  <h1>Archive theme: {data.kind}</h1>
  <p>First-phase placeholder. Presentation design is intentionally deferred.</p>
  {data.kind === "post" && <p>{data.post.metadata.title}</p>}
  {Body && <Body />}
</main>
