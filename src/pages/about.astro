---
import { getActiveTheme, type AboutPageData } from "@core/theme";

const theme = getActiveTheme();
const Page = theme.pages.about;
const data: AboutPageData = { kind: "about" };
---

<Page data={data} />
