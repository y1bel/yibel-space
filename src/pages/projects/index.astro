---
import { getProjects } from "@core/content";
import { getActiveTheme, type ProjectsPageData } from "@core/theme";

const theme = getActiveTheme();
const Page = theme.pages.projects;
const projects = await getProjects();
const data: ProjectsPageData = { kind: "projects", projects };
---

<Page data={data} />
