---
import { getTimeline } from "@core/content";
import { getActiveTheme, type TimelinePageData } from "@core/theme";

const theme = getActiveTheme();
const Page = theme.pages.timeline;
const timeline = await getTimeline();
const data: TimelinePageData = { kind: "timeline", timeline };
---

<Page data={data} />
