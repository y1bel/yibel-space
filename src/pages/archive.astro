---
import { getPosts } from "@core/content";
import { getActiveTheme, type ArchivePageData } from "@core/theme";

const theme = getActiveTheme();
const Page = theme.pages.archive;
const posts = await getPosts();
const data: ArchivePageData = { kind: "archive", posts };
---

<Page data={data} />
