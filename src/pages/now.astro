---
import { getActiveTheme, type NowPageData } from "@core/theme";

const theme = getActiveTheme();
const Page = theme.pages.now;
const data: NowPageData = { kind: "now" };
---

<Page data={data} />
