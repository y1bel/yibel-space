# Yibel Space

First-phase Astro architecture scaffold for a theme-decoupled personal digital space.

The default build-time theme is configured in `src/config/theme.ts`. The two registered themes deliberately render text-only placeholders; Archive UI development begins after this scaffold is accepted.

## Commands

```bash
npm install
npm run build
```
